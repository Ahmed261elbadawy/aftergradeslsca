PORT=3000
ADMIN_PASSWORD=change-me
SESSION_SECRET=change-this-secret

# Set automatically by Vercel when you attach a Postgres database and a Blob store.
# Leave unset for local development (falls back to SQLite + local disk).
# POSTGRES_URL=
# BLOB_READ_WRITE_TOKEN=
